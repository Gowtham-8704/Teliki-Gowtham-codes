#include<stdio.h>
int main ( )
/* C program for swaping of two numbers without using a third variable */
{
	int a,b,c,d;
	printf("enter the two numbers");
	scanf("%d %d",&a,&b);
	printf("the value of a after swaping is %d",b);
	printf("the value of b after swaping is %d",a);
	
	return 0;
}
